import { KEYS } from './settings';

const KEYBOARD_KEYS = [
  ['q', 'w', 'e', 'r', 't', 'y', 'u', 'i', 'o', 'p'],
  ['a', 's', 'd', 'f', 'g', 'h', 'j', 'k', 'l'],
  [KEYS.ENTER, 'z', 'x', 'c', 'v', 'b', 'n', 'm', KEYS.BACKSPACE],
];

export default KEYBOARD_KEYS;
